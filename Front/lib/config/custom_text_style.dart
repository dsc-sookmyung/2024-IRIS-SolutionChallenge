
import 'package:flutter/material.dart';

class CustomTextStyle {
  // * normal fontSize: 14 *
  // large + bold
  static const titleBold= TextStyle(fontSize: 20, fontWeight: FontWeight.bold);
  // large + (normal)
  static const title= TextStyle(fontSize: 20);
  // medium + bold
  static const basicBold= TextStyle(fontSize: 16, fontWeight: FontWeight.w600);
  // medium + (normal)
  static const basic= TextStyle(fontSize: 16);
  // small + (normal)
  static const small= TextStyle(fontSize: 12);
}